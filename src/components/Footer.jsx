import React from "react";

const Footer = () => {
  return (
    <footer>
      <form>
        <input placeholder="Add todo" />
        <button>Add</button>
      </form>
    </footer>
  );
};

export default Footer;
